services:
  web:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "80:8000"         # http://<IP_VPS>
    restart: unless-stopped
    volumes:
      - ./backend:/app
